'use strict';

describe('myApp.present module', function() {

  beforeEach(module('myApp.present'));

  describe('present controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var presentCtrl = $controller('presentCtrl');
      expect(presentCtrl).toBeDefined();
    }));

  });
});