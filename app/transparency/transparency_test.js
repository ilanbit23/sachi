'use strict';

describe('myApp.transparency module', function() {

  beforeEach(module('myApp.transparency'));

  describe('transparency controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var transparencyCtrl = $controller('transparencyCtrl');
      expect(transparencyCtrl).toBeDefined();
    }));

  });
});